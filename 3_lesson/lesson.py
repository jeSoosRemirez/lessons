#ITVDN
#Create calculator
#PEP 8
#Pull Requests
#Function
#Арк і кваркс

#
def add (first_num, second_num):
    return (first_num + second_num) * 4
result = add(2, 5)
print(result)

#First
def not_a_zero(number):
    if number != 0:
        return True
if not_a_zero(3)
    print('not_a_zero')

#Second
def not_a_zero(number):
    if number != 0:
        return True
if not_a_zero(0)
    print('not_a_zero')

#Third
def not_a_zero(number, c = 6)
    print(number + c)
not_a_zero(0)
not_a_zero(6, 3)

#Function Lambda
func_lambda = lambda x, y: x + y
def norm_func(x, y):
    return x + y
print(func_lambda(1, 2))
print(norm_func(1, 2))

#
def some_func():
    a = 1
    b = 2
print(a)
